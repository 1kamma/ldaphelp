# Tests

This repository now includes:

- root-package Go unit tests for schema parsing and binary value helpers
- a root-package end-to-end style handler render test for the browse UI

This directory is reserved for future shared test fixtures and browser-level assets if the project grows a larger dedicated E2E harness.
